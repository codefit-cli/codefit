package dbcoverage

// Deterministic returns the DB dimension's deterministic coverage prose — the
// classes the DB rules affirm with certainty 1.0.
func Deterministic() []string {
	return []string{
		"Database structure — table without a primary key (DB-050): a model with no @id and no @@id. A table with no primary key is a structurally undeniable defect, so it is affirmed deterministically — BUT ONLY over a table whose structure the parser could PROVE complete (db.Table.Complete, ADR 0034, db-model-completeness-contract). When one or more statements affecting a table could not be reduced (a dropped ALTER TABLE shape, a malformed CREATE TABLE body, an unrecognized Prisma model-body line), DB-050 does NOT affirm — it ROUTES that table to a dedicated db-table-structure-unproven surface item instead (see Reasoning, below), carrying the raw unreduced statement and file:line so the agent can read the source DDL itself. \"Absence of DATA\" (a genuinely missing key) and \"absence of FEATURE\" (a key the parser could not read) are DIFFERENT claims, and DB-050 never blurs them. Read from the configured schema — a Prisma schema.prisma OR a directory of SQL-DDL (Flyway) migrations reconstructed to their final state (database.schema_paths). SQL-DDL parsing supports THREE dialects — PostgreSQL, MySQL, and SQL Server (T-SQL) — selected by the `database.type` setting in .codefit.yaml (postgresql | mysql | sqlserver); `sqlite` is recognized but returns an explicit \"not supported yet\" note rather than silently parsing as Postgres. All DB rules (DB-050 and below) are dialect-agnostic: they reason over the neutral db.Schema model reconstructed by whichever dialect parsed the DDL, never over dialect-specific fields, so the same rule set runs identically regardless of dialect. An unmapped type keyword in the source DDL (one the dialect's type vocabulary does not recognize) falls back to db.TypeUnknown — an honest fallback, never a silent guess. A TEMPORARY table (PostgreSQL/MySQL TEMP|TEMPORARY, T-SQL's #/## name prefix) is deliberately EXCLUDED from the model entirely (ADR 0043), so DB-050 and every other DB/DW rule never sees one: a session-scoped table is dropped with its session and is not part of the persistent schema. That withholding is never silent — the per-scan note states how many declarations were withheld and why. The DB dimension covers only what the schema states, not query behavior.",
	}
}

// Reasoning returns the DB dimension's coverage prose mapped as surface for the
// agent to reason over — schema-only structural facts codefit does not affirm.
func Reasoning() []string {
	return []string{
		"Database structure from the schema, mapped as surface for the agent to reason (read from database.schema_paths; schema-only, no query analysis): foreign keys with no covering index (DB-001) — a FK is covered when some index's leading columns match it (the primary key counts as an implicit index, a @unique as an index); whether an un-indexed FK matters depends on the table's size and access pattern, so codefit states the fact (fk_columns, existing_indexes, covering_index_detected=false) and the agent judges. Exact duplicate indexes (DB-011a) — two indexes on the same columns in the same order with the same uniqueness; which to drop is the human's call. Real-world coverage is dialect-uneven: on PostgreSQL/Pagila this rule fires on a GENUINE upstream duplicate (the payment_p2022_01 partition ships both idx_fk_payment_p2022_01_customer_id and payment_p2022_01_customer_id_idx on the identical column) — the only one of the three dialects where the positive case is proven by real vendored DDL rather than a constructed one; on MySQL/Sakila and SQL Server/AdventureWorks the rule is verified CLEAN against real DDL (no false positives), but its positive fire path is proven only by a constructed (synthetic) schema, since neither real corpus happens to ship a duplicate index. Multivalued (array) columns (DB-002) — an array violates 1NF, but a native array (Postgres) is legitimate sometimes, so it is surfaced not affirmed.",
		"Prefix-redundant indexes (DB-011b, Phase 2.2) — an index [a] that is a STRICT leading prefix of a wider index-like coverer [a,b] on the same table (a real index, or the primary key treated as an implicit index) is pure write/storage overhead, since the wider one already serves any lookup the narrower one would; which to drop is the human's call, so this is surface, never an affirmation. A UNIQUE index never fires (it enforces a uniqueness constraint the wider composite does not guarantee for its own leading columns alone). Supported on all three dialects — PostgreSQL/Pagila, MySQL/Sakila, and SQL Server/AdventureWorks all run CLEAN against their real vendored DDL (none of the three real corpora happens to contain a genuine prefix-redundant pair, a real honest finding about how these schemas are actually indexed, not a rule gap); the positive fire path on every dialect is proven by mutating a copy of a real table (e.g. adding a synthetic index on the leading column of Sakila's own real film_actor composite primary key [actor_id, film_id]), never by a fully synthetic fixture.",
		// PLACEMENT, stated rather than buried: the code×schema cross rules live in
		// internal/core/crossrules, not internal/core/dbrules, so their prose sits in
		// dbcoverage only because their output IS part of the DB dimension's — their
		// categories are unioned into the db baseline scope (ADR 0019/0029) and they are
		// reported in scan-all's DB section. Whether the cross deserves its own neutral
		// coverage leaf (a crosscoverage package, composed by append exactly as
		// dbcoverage is) remains an OPEN placement question. Appending here is reversible:
		// dbcoverage itself was relocated out of providers/typescript/coverage.go in 0.2.2.
		"Index-vs-query — the code's queries CROSSED with the schema (DB-010/DB-013, ADR 0029/0030/0031; scan-all only, Prisma only). Unlike every other DB entry here, these two do NOT reason over the schema alone: they read BOTH neutral inputs — the WHERE columns of the code's Prisma queries AND the schema's indexes — which is why they are a separate rule family with its own runner. Both are pure SURFACE, never affirmations: the structural fact (filtered-and-uncovered) is certain once the join matches, but whether a missing index MATTERS depends on the column's cardinality, the table's size and the write load, the same agent judgment DB-001 already defers. \"Covered\" is delegated to the SAME shared helpers of the neutral model that DB-001 and DW-010 use (db.IndexLike / db.CoveredByOrderedPrefix / db.CoveredBySetPrefix / db.UniqueKeys), so \"what serves a lookup\" is defined ONCE and cannot drift between rules: an index's LEADING columns count, the primary key counts as an implicit index, a @unique counts, and a filter that constrains a unique key is a single-row lookup that never fires. THE CERTAINTY GATE: the code↔schema join resolves a filter only when EXACTLY one table matches its model name and at least one filtered name is a real column of it — matching is by LOGICAL field name on both sides (a Prisma @map physical name never enters), exact and never fuzzy or case-insensitive, because all naming-convention knowledge (the Prisma Client accessor \"user\" → model \"User\") lives in the provider. Anything ambiguous or unmatched ABSTAINS and emits nothing — the Complete==false floor applied to the cross: a mutilated cross is worse than an absent one. (1) Filtered column with no index (DB-010): ONE column the code filters on that nothing — index, @unique, or primary key — covers as a LEADING column (a composite index [a,b] covers a lookup on a, not on b). Deduplicated per (table, column), so two queries filtering the same unindexed column are one concern reported once. A Boolean or enum column is deliberately SKIPPED: bounded by its declared type, where a standalone index is almost always wrong. The item anchors to the SCHEMA column — the filtering query is context, the fix is a schema change. (2) Multi-column filter with no composite index (DB-013): a WHERE a AND b with no composite index holding that column SET as its leading columns. Coverage here is ORDER-INSENSITIVE — [b,a] covers (a,b) — because codefit does not capture the WHERE operator and so cannot reason about equality-vs-range column order; it reports the set and leaves the order to the agent. It ABSTAINS at four or more filtered columns (which subset to index needs selectivity codefit cannot observe, so it says nothing rather than emitting a guess in an item's clothing). The same uncovered column set recurring across many models is GROUPED into ONE item naming every affected model — (salon_id, deleted_at) across fifteen models is one architectural observation (tenant scoping plus soft delete), not fifteen findings — anchored on the first model alphabetically, with the column set carried as the item's snippet so the baseline fingerprint stays stable no matter which model anchors it. PRECEDENCE, by construction: the two rules partition every resolved filter by its real column count — DB-010 owns the single-column ones, DB-013 the multi-column ones — so they never double-report one filter and neither inspects the other's output; a multi-column WHERE is deferred WHOLE to DB-013 rather than split into a standalone index per column. SCOPE: the code side needs a language provider that extracts query filters, and the TypeScript provider reading Prisma call-sites is the only one today; it reads the call's top-level where SPECIFICALLY, so a data/select/include/orderBy object whose keys look like columns is never mistaken for a filter, and a top-level AND is recursed and merged while OR/NOT are a declared, skipped limit. The cross runs inside scan-all only — the standalone codefit-scan-db tool is schema-only — and its categories are unioned into the db-dimension baseline scope so its items are tracked and pruned like every other db item. VOLUME scales with the schema, measured on dogfooded Prisma projects rather than assumed: roughly 7 items on 18 models, 21 on 40, 57 on 77 — a steady ~1/3 of the DB channel, proportional rather than runaway.",
		"Database structure — name-heuristic checks, mapped as surface (schema-only). These read meaning from column names, which is inherently noisy, so they are NEVER affirmed: codefit states the structural fact and the agent judges. (1) Foreign key typed as text (DB-051): a String/Text FK whose referenced key is numeric, or an unbounded @db.Text key — a String FK to a String uuid/cuid key does NOT fire (it is a structural type-mismatch check, not a name guess), and an unresolvable reference does not fire. Facts: type_mismatch, text_key, referenced_type_resolved. (2) Missing audit timestamps (DB-052): a table with NEITHER createdAt NOR updatedAt; looks_like_join_table is exposed so link tables can be dismissed. Only-one-missing is a deferred candidate, not fired yet. (3) Sensitive column stored in the clear (DB-053): a column whose name matches a sensitive token (password, token, apiKey, ssn, …) and whose type holds a value (String/Text/Bytes). It ALWAYS emits; an encryption hint in the name (passwordHash, encrypted…) is reported as the fact encryption_hint_in_name, NOT used to suppress — a name is not a guarantee, and hiding a possible plaintext secret would be a silent false negative. Names are matched by component (camelCase/snake_case), never raw substring, so passwordChangedAt (a DateTime) and passwordResetCount (an Int) do not fire. (4) Repeating groups (DB-003): two or more same-typed columns sharing a base name with numeric suffixes (phone1/phone2/phone3) — a 1NF smell the agent weighs against an intentional fixed set (address line 1/2).",
		"View sensitive-column exposure (DB-020, Phase 2.2) — a VIEW whose top-level SELECT column list exposes a column or alias whose name matches a sensitive token (the same vocabulary DB-053 uses). Reads the view's captured body text through a deliberately bounded SELECT-projection scanner, never a general SQL-expression parser: a function call, CASE expression, or subquery item with no alias is a declared miss for that one item, and SELECT * is a declared miss for the whole view (the column list is not literal in the DDL text). Never an affirmation — a column named password in a view says nothing about whether the underlying value is masked, hashed, or genuinely exposed to whoever queries the view, so this is always surface. Runs CLEAN on real DDL across all three dialects: Pagila's actor_info, AdventureWorks' vEmployee, and Sakila's customer_list all legitimately confirm the negative case (a real, well-designed view's whole purpose is usually a curated projection, so it rarely re-exposes a raw sensitive column under a sensitive-looking name — a real finding about how views are written, not a gap in the rule). The POSITIVE fire path is proven ONLY by a CONSTRUCTED case: renaming the real, vendored Sakila customer_list view's own trailing \"AS SID\" alias to \"AS ssn\". No real view in any of the three dogfooded corpora genuinely exposes a sensitive-named column — that is stated plainly, not implied by the negative runs above. Zero value on Prisma-only projects: Prisma's schema.prisma has no view-block concept (ADR 0014 places it out of scope), so a Prisma-only project has no views for this rule to read at all.",
		"Routine without exception handling (DB-031, 0.2.3 routine-body rules) — a stored procedure OR function (both surface as db.Procedure) whose captured body contains NO exception-handling construct for its dialect: T-SQL BEGIN TRY (paired with BEGIN CATCH), MySQL DECLARE ... HANDLER, or PL/pgSQL EXCEPTION WHEN. This states an ABSENCE as a structural fact, NEVER an affirmation of a defect: whether the missing handler matters is the agent's judgment, and so is the ADEQUACY of a handler that IS present — an empty CATCH, or a handler that swallows every error, reads as \"present\" here (the construct exists) yet may still be a real bug. DB-031 detects the STRUCTURE, it does not grade it. Read through a deliberately bounded, string/comment-aware token scanner (the same declared-subset discipline as DB-020), never a general SQL parser: a handler-shaped token inside a string literal or a comment does not false-match. CRITICAL: PostgreSQL RAISE EXCEPTION is a THROW, not a handler — the scanner matches EXCEPTION only when the very next keyword token is WHEN, so RAISE EXCEPTION and a bare EXCEPTION never count as handling. Gated on Body.Complete (ADR 0004/0025): a body the parser could not prove whole is never evaluated, so an absence over unread/truncated text is never falsely affirmed. Per-dialect coverage against REAL vendored DDL: MySQL/Sakila — real POSITIVE (rewards_report, no HANDLER) and real NEGATIVE (inventory_held_by_customer, real DECLARE EXIT HANDLER FOR NOT FOUND); T-SQL/AdventureWorks — real POSITIVE (uspGetBillOfMaterials, a recursive-CTE body with no TRY/CATCH) and real NEGATIVE (uspUpdateEmployeePersonalInfo, real BEGIN TRY ... BEGIN CATCH); PostgreSQL — real POSITIVE (Pagila rewards_report, which RAISEs EXCEPTION twice but has NO EXCEPTION WHEN — the bare-EXCEPTION trap, locked by an explicit test) plus a CONSTRUCTED NEGATIVE, declared synthetic in testdata/pg_constructed_exception_handler.sql (a small hand-written safe_divide function with a real BEGIN ... EXCEPTION WHEN ... END block), since no routine in the dogfooded Pagila excerpt contains a real handler clause. TRIGGERS are intentionally out of scope for DB-031 (their cross-table/external-effect risks are DB-040/DB-041's domain). Zero value on Prisma-only projects: Prisma's schema.prisma has no stored-procedure/function block concept, so a Prisma-only project has no routines for this rule to read.",
		"Trigger cross-table cascade (DB-040, 0.2.3 routine-body rules) — a TRIGGER whose body performs DML (INSERT/UPDATE/DELETE) against a table OTHER than the one it fires on. Surface, never an affirmation: it states the structural facts (writes_other_table: X for each other table, and documented_by_comment) and the AGENT judges whether the cascade is intentional and correct — DB-040 detects the cross-table write, it does not grade whether the cascade is right. Body source is per-dialect (ADR 0026): MySQL/T-SQL triggers carry an INLINE body scanned directly; a PostgreSQL trigger has NO inline body, so the rule resolves Schema.ExecutedProcedure(trigger) to the executed function and scans ITS body, still comparing writes against the trigger's own table — an unresolvable built-in (e.g. tsvector_update_trigger) makes the rule abstain honestly (it cannot see the logic). Bounded string/comment-aware scanner (the DB-020/DB-031 declared-subset discipline), never a SQL-expression parser: a DML-shaped token inside a string literal or comment does not fabricate a write; the T-SQL UPDATE(column) column-changed function is excluded (it names no table); the trigger's own event clause (AFTER UPDATE, INSTEAD OF DELETE, INSERT OR UPDATE) is not mistaken for a statement; and a schema-qualified write to the trigger's OWN table (\"Purchasing\".\"PurchaseOrderDetail\" on a trigger for PurchaseOrderDetail) is correctly NOT a cascade. Gated on Body.Complete (ADR 0004/0025): a body the parser could not prove whole is never evaluated. Per-dialect coverage: T-SQL/AdventureWorks — real POSITIVE (uPurchaseOrderDetail cascades into TransactionHistory and PurchaseOrderHeader, while its same-table PurchaseOrderDetail UPDATE is correctly excluded) and real NEGATIVE (dEmployee, an INSTEAD OF DELETE that only RAISERRORs and ROLLBACKs); MySQL/Sakila — real POSITIVE (ins_film/upd_film/del_film each cascade a write into film_text) and a CONSTRUCTED NEGATIVE declared synthetic in testdata/mysql/constructed_non_cascading_trigger.sql (a trigger that only stamps NEW.created_at), since every real Sakila trigger cascades; PostgreSQL — real NEGATIVE (Pagila last_updated, whose function only sets NEW.last_update) and a CONSTRUCTED POSITIVE declared synthetic in testdata/pg_constructed_cascade_trigger.sql (a trigger→function pair whose function INSERTs into an order_audit table, exercising the ADR-0026 resolution path), since no real Pagila trigger writes another table. Zero value on Prisma-only projects: Prisma's schema.prisma has no trigger block concept.",
		"Trigger external-effecting call (DB-041, 0.2.3 routine-body rules) — a TRIGGER whose body invokes a call that reaches OUTSIDE the database (a shell exec, OLE automation, email, a remote/cross-database query, an async notification, or a pipe to a program). Surface, never an affirmation: it states the fact (external_call: X) and the AGENT judges whether the call is safe — DB-041 detects the external call, it does not decide whether it is exploitable. STRICT vocabulary is the line between real risk and noise: an EXECUTE/CALL of an INTERNAL stored procedure (EXECUTE dbo.uspLogError, CALL recompute_totals) is a normal routine call and does NOT fire — that is the rule's trap, the token that looks like a call but is not external. Per-dialect vocabulary: T-SQL (xp_cmdshell, sp_OA* OLE automation, sp_send_dbmail, OPENROWSET, OPENQUERY); PostgreSQL (dblink/dblink_exec, NOTIFY/pg_notify, COPY ... PROGRAM); MySQL (sys_exec/sys_eval UDFs). Body source is per-dialect, resolving a PostgreSQL trigger to its executed function (ADR 0026) exactly as DB-040 does; gated on Body.Complete. Bounded string/comment-aware scanner: an external-call token inside a string literal or comment does not fire, and a member access (NEW.notify) is not mistaken for the NOTIFY statement. Per-dialect coverage: T-SQL/AdventureWorks — a CONSTRUCTED POSITIVE declared synthetic in testdata/tsql/constructed_external_call_trigger.sql (a trigger that EXECs xp_cmdshell) and a real NEGATIVE / trap (uPurchaseOrderDetail, whose only calls are EXECUTE of the INTERNAL uspPrintError/uspLogError logging procs — not external); PostgreSQL — a CONSTRUCTED POSITIVE declared synthetic in testdata/pg_constructed_external_call_trigger.sql (a trigger→function pair whose function issues NOTIFY, exercising the ADR-0026 resolution path) and a real NEGATIVE (Pagila last_updated); MySQL — DETECTABLE WITHOUT DOGFOOD: the scanner recognizes the MySQL external vocabulary (sys_exec/sys_eval) and WOULD fire if one appeared, but a trigger making an external call is structurally rare and non-idiomatic in MySQL (no NOTIFY, external effects need exotic UDFs), so no real or constructed MySQL case is dogfooded. This is DISTINCT from NotCovered (which would mean non-detectable): the capability exists, only the dogfood evidence is absent by the dialect's nature — stated plainly rather than faked with an unrealistic constructed case. Zero value on Prisma-only projects: Prisma's schema.prisma has no trigger block concept.",
		"Dynamic SQL construction in a routine (DB-030, 0.2.3 routine-body rules) — a stored procedure OR function (both surface as db.Procedure) whose body BUILDS and runs SQL from a string at runtime. Surface, never an affirmation: it states the fact that dynamic SQL is constructed (dynamic_sql: <marker>) and the AGENT judges whether it is injectable — codefit maps the surface, it deliberately does NOT do taint analysis (whether the string mixes in untrusted input is the agent's reasoning over the surface). THE TRAP: a static EXEC/CALL of a NAMED internal procedure (EXEC dbo.uspFoo, CALL recompute_totals) is NOT dynamic SQL and does not fire — the same EXEC family as DB-041's trap, excluded for a different reason (there 'not external', here 'not dynamic'; an EXEC dbo.uspLogError fires neither). Per-dialect markers: T-SQL (sp_executesql; EXEC(<expr>)/EXECUTE(<expr>) in parentheses, NOT a literal proc name); PL/pgSQL (EXECUTE '<string>' / EXECUTE format(...); quote_literal / quote_ident string building); MySQL (PREPARE ... FROM a built string). Bounded string/comment-aware scanner; gated on Body.Complete (ADR 0004/0025). Declared limit: a bare EXECUTE <variable> with no construction marker visible in the same body is a miss (the string may have been built out of view) — a known gap, not a silent one. Per-dialect coverage: PostgreSQL — real POSITIVE (Pagila rewards_report builds a query string with quote_literal and || concatenation and runs it via EXECUTE) and real NEGATIVE (last_updated); MySQL — a CONSTRUCTED POSITIVE declared synthetic in testdata/mysql/constructed_dynamic_sql_proc.sql (PREPARE ... FROM a CONCATenated string) and a real NEGATIVE (Sakila rewards_report, temp-table based, no dynamic SQL); T-SQL — a CONSTRUCTED POSITIVE declared synthetic in testdata/tsql/constructed_dynamic_sql_proc.sql (sp_executesql over a built string) and a real NEGATIVE (uspGetBillOfMaterials, a recursive CTE). Zero value on Prisma-only projects: Prisma's schema.prisma has no stored-procedure/function block concept.",
		"Star-schema and slowly-changing-dimension checks (DW-001/002/005/010/011, S2 of the RF-03 OLAP closure) — five rules that read the schema PLUS the S1 paradigm/table-role classification, and that reach ONLY tables classified fact- or dimension-role; an oltp or unclassified table is never evaluated by any of them. All five are pure SURFACE, never affirmations (ADR 0017): a warehouse-modelling choice is a design judgment, not a structurally undeniable defect, so codefit states the observed shape and the agent decides. (1) Fact table with no dimension foreign key (DW-001): a fact-role table whose foreign keys reach no dimension-role table. A foreign key to another FACT, to staging, or to an unclassified table deliberately does NOT count — a fact-to-fact bridge looks joined but carries no dimensional context. A fact with zero foreign keys fires too, and says so (foreign_keys: (none)) rather than silently passing on an empty list. (2) Dimension without a surrogate key (DW-002): a dimension-role table whose primary key is composite, or a single column that is not provably an integer surrogate. The surrogate test is structural and narrow on purpose — a one-column primary key of integer type, the shape every well-modelled warehouse uses — so no name guessing is involved. DECLARED LIMIT (a): a UUID/GUID surrogate types as a string in the neutral model and therefore FIRES; the emitted facts (composite_primary_key, integer_primary_key, primary_key_column_resolved) are what the agent needs to dismiss it in one step. DECLARED LIMIT (b), the incomplete-reconstruction path: when the primary key names a SINGLE column that the parser did not reconstruct onto the table, the surrogate test cannot be satisfied — proving an integer surrogate requires reading the column's type — so the rule FIRES as not-provably-a-surrogate and names the reason rather than hiding it: primary_key_column_resolved=false, with the primary_key_type signal reading \"(column not declared in the parsed schema)\". This is deliberate and test-locked, and it is the OPPOSITE choice from DB-051, where an unresolvable reference does NOT fire: DB-051 compares a FK's type against the referenced key's type and has nothing to compare when the reference does not resolve, whereas DW-002 asks whether a surrogate is PROVEN, and an unread column proves nothing. The state is reachable, not hypothetical: SQL-DDL known limit (5) below drops a column whose name is an index keyword and whose type is outside the dialect's vocabulary (Pagila's real film.fulltext), while a table-level PRIMARY KEY (fulltext) naming it survives into the model. A dimension with NO primary key at all ABSTAINS — DB-050 already affirms that case deterministically, and two IDs for one defect is noise. (3) Facts present, no time dimension (DW-005): schema-level, at most ONE item anchored on the first fact table. A time dimension is recognized by EITHER the NAME or the structural grain. The NAME test COMPOSES on the SAME role-name vocabulary the paradigm/table-role entry below documents: it strips the recognized warehouse role token off either end of the table name and checks that what REMAINS is exactly date, time or calendar (separator-insensitive), so dim_date, D_DATE, d_date, date_dim, DATE_DIM, DimDate and DimCalendar are all recognized. Composing rather than keeping a second spelling list is a FIX, not a flourish: this rule used to carry its own hardcoded dim_date/dim_time/dim_calendar, and when the role vocabulary widened the two drifted — two real corpora spelling their calendar D_DATE and D_Date began classifying as dimensions while staying invisible HERE, so DW-005 reported \"this fact table reaches no time dimension\" over schemas that plainly declare one, turning a silent miss into a confident false claim. The remainder is matched by EQUALITY, never containment: separators are stripped before comparison, so a substring test for \"date\" would swallow dim_update, dim_candidate and dim_validate and SILENCE the rule on a warehouse that genuinely has no calendar. The structural grain is a dimension whose primary key is a single date/datetime column, which is date-grained by construction; that signal keys on the PRIMARY KEY, not on \"contains a date column\": an updated_at audit stamp is not a calendar, and accepting any date column would suppress the rule on almost every schema — a silent false negative. DECLARED LIMITS, both in the MISS direction by design (the rule asks a question the agent can answer from the schema rather than making a claim codefit cannot back): (a) a calendar keyed by an integer yyyymmdd smart key (AdventureWorksDW's own DimDate) is recognized only by NAME, because that key is structurally indistinguishable from any other surrogate; (b) by name, ONLY a role token plus exactly date/time/calendar matches — a spelled-out or qualified calendar name (date_dimension, dim_date_full, dim_fiscal_date, dim_datetime) does NOT, and neither does a bare \"calendar\" carrying no role token, which the role vocabulary would never classify as a dimension in the first place. (4) SCD-2 dimension without a currency index (DW-010): a dimension carrying slowly-changing columns (valid_from/valid_to/is_current/effective_date, separator-insensitive so validTo/isCurrent are matched too) where no index leads with valid_to or is_current — so every \"give me the current version\" query scans the whole version history, the part that grows without bound. Index coverage is delegated to the SAME shared helpers DB-001 and DB-010 use (db.IndexLike / db.CoveredByOrderedPrefix), so \"what serves a lookup\" is defined once in the neutral model: the primary key counts as an implicit index, and a composite index NOT leading on the currency column does not cover it. EITHER currency column being covered is enough to go quiet. A dimension with no slowly-changing columns is never evaluated; one with history columns but NEITHER currency column abstains rather than demanding an index no query would use. (5) Mixed SCD strategies (DW-011): schema-level, one item, when some dimensions keep history and others overwrite in place — a report joining both mixes point-in-time with as-of-today attributes. TIME DIMENSIONS ARE EXCLUDED from the comparison (a calendar is not slowly-changing by definition), because counting one as SCD-1 would fire this rule on essentially every correctly built warehouse; a genuine split between two other dimensions still fires. The exclusion uses the SAME time-dimension test as DW-005 above, so widening one never leaves the other behind. DECLARED PARTITION CHILDREN ARE EXCLUDED TOO, from DW-005's and DW-011's censuses alike (ADR 0039): a PostgreSQL `CREATE TABLE c PARTITION OF p` child declares its bounds and nothing else, so it carries no columns of its own and is a restatement of its parent rather than an independent fact or dimension. Counting one would inflate DW-005's fact/dimension lists by one name per partition (a five-year monthly range is 60), and would hand DW-011 a dimension with no history markers — an SCD-1 dimension fabricated out of a partition, which on a uniformly SCD-2 warehouse is enough to make DW-011 report a mix that does not exist. The same predicate scopes each rule's completeness gate, which is what stopped both rules from going silent on every declaratively partitioned warehouse. DW-005 keeps ONE reading of an excluded child: when the child's PARENT is calendar-NAMED, the schema HAS a time dimension. That is not a second vocabulary — it is timeDimensionName applied to db.Partitioning.Of — and it exists because a warehouse that partitions its calendar and has its fact reference a specific partition (required before PostgreSQL 12) loses the parent's dimension role to ADR 0033's fan-in corroboration, leaving DW-005 about to claim there is no calendar in a schema that declares dim_date on its face. DW-010 and DW-011 share one history vocabulary so they cannot disagree about what \"keeps history\" means, and share its declared limit: a dimension using a different vocabulary (AdventureWorksDW's DimProduct uses StartDate/EndDate/Status) reads as SCD-1. DOGFOOD STATUS, stated plainly rather than implied: the positive and trap fire paths of all five rules are proven by CONSTRUCTED (declared synthetic, ADR 0028) schemas, not by real vendored DDL. Microsoft's AdventureWorksDW IS vendored (testdata/tsql/adventureworksdw_real_objects.sql, MIT) and it IS now reached, AS VENDORED, under Microsoft's own names, with no rename or mutation of any kind — the first time this family measures anything on real third-party warehouse DDL. BOTH of the reasons it used to yield nothing are closed: the NAMING half by the role-name vocabulary (it recognizes the PascalCase Kimball spelling FactInternetSales/DimCustomer), and the PARSER half by the reducer now reading the ALTER TABLE ... ADD CONSTRAINT shapes this DDL uses — its three real primary keys and all eight real foreign keys are in the model, every table is StructureProven, the schema classifies olap, and the roles come out fact / dimension / dimension. Locked positively over the real parsed corpus by TestDW_AdventureWorksDW_StarIsVisible_AsVendored (internal/providers/sqlddl/dw_integration_test.go); Classification.Unprovable is EMPTY there now, which is the advance, not a regression — a recognized name lands in Unprovable only while structure cannot corroborate it. WHAT IT ACTUALLY YIELDS, stated exactly rather than as a win: NONE of these five S2 rules fires on it. DW-002 used to fire twice (DimCustomer and DimDate), and it fired for a PARSER reason rather than a modelling defect — both keys ARE single-column integer surrogates, but the delimited T-SQL type name ([int]) read as TypeUnknown, so \"provably an integer\" could not be satisfied. That was SQL-DDL known limit (8), and it is CLOSED (see (8) in NotCovered): all 74 of this corpus's columns now classify, CustomerKey and DateKey are proven integer surrogates, and the two items are gone. Both DIRECTIONS are test-locked in internal/providers/sqlddl/dw_integration_test.go (TestDW002_AdventureWorksDW_SurrogateKeysAreProven asserts the [int] types AND the absence of any dw-dimension-no-surrogate-key item), because the type assertion alone would pass over a rule that stopped reading types and the absence assertion alone would pass over a rule that went globally silent. DW-001 does not fire (the fact reaches both vendored dimensions), DW-005 does not fire (DimDate is recognized by name), and DW-010/DW-011 do not fire (this excerpt carries no SCD columns). So the corpus proves the family REACHES real warehouse DDL end to end; it does not yet carry the five rules' positive fire paths, which stay on constructed fixtures per ADR 0028. ZERO VALUE ON PRISMA: a schema.prisma expresses no warehouse concept, so these rules can only classify a Prisma project whose models happen to carry a recognized warehouse name.",
		"Table structural completeness (db-model-completeness-contract, ADR 0034) — every table carries a completeness signal (db.Table.Complete/Note/Unreduced) mirroring the pre-existing Body.Complete idiom (ADR 0004/0025) at TABLE granularity: false means the parser met at least one statement affecting the table it could NOT reduce and could not rule out as declaring a key/index/column. A DECLARED, RECOGNIZED skip (CHECK/EXCLUDE constraints and MySQL's inline PARTITION-definition body items; ALTER COLUMN/RENAME/OWNER/ENABLE/DISABLE/CLUSTER/SET/RESET/VALIDATE/NO alter actions) is NOT incompleteness — recording those would mute the whole DB dimension on ordinary PostgreSQL DDL, so only a GENUINELY unrecognized statement demotes a table. A table's PARTITIONING CLAUSE is no longer among those skips at all: as of partition-capture the reducer READS it into db.Table.Partitioning (see the SQL-DDL known limits below), and reading it NEVER demotes a table — measured through the real parser across all 17 vendored corpora, whose table counts and structure-proven counts are identical before and after that slice. On the Prisma path, a model-body line that is neither a recognized field nor a @@-attribute demotes the table the same way; the deferred, unrelated top-level-line skip (a view block) does not. Two-way disposition, no rule signature change (ADR 0015): DB-050 (the dimension's one AFFIRMATION) ROUTES an unproven table to the db-table-structure-unproven surface category instead of affirming; DB-001, DB-052, and all seven DW-0xx rules (DW-001/002/005/010/011/020/021) ABSTAIN silently on an unproven table (a dropped statement might have declared the very index/column/key each rule is asking about). One reason reaches that same disposition WITHOUT any parser failure: a PostgreSQL PARTITION CHILD (CREATE TABLE c PARTITION OF p FOR VALUES ...) is registered as its own table and marked unproven under ReasonPartitionChildInheritsStructure, because that statement declares the child's partition bounds and NOTHING else — its columns, primary key and constraints all live on the parent. Before partition-capture the child table did not enter the model at all: the statement matched no dispatch branch and the whole table vanished silently, which is strictly worse than an unproven one. DW-005, DW-011 and DW-020 are SCHEMA-LEVEL census judgments and abstain the WHOLE rule — never a per-table skip, which would silently shrink the census and still emit. ALL THREE now scope that whole-rule gate to their own census MEMBERS, so a declared PARTITION CHILD is EXEMPT from it (ADR 0038, generalized by ADR 0039): a child is not in any of their censuses in the first place, and its unprovenness is by construction rather than a parser failure, so gating on it abstained those rules on precisely the warehouses that DO partition. DW-020 shipped with that exemption; DW-005 and DW-011 did not, and the cost was MEASURED rather than reasoned — adding one fact-role partition child to a star made DW-005 stop emitting dw-no-time-dimension over it (ADR 0038 §4 recorded that as an open false negative), and a DIMENSION partition child, which ADR 0038 believed impossible to reach DW-011, did the same to dw-mixed-scd-strategies. The exclusion is enforced through ONE membership predicate per rule, consulted by both the gate and the census loop (internal/core/dwrules/census.go), so a table can never be gated without being censused. Excluding a child from the census is not optional bookkeeping either: a child declares no columns of its own, so counting one would have fabricated an SCD-1 dimension for DW-011 and inflated DW-005's fact/dimension lists by one name per partition. DW-005 pays for its own exclusion where the exclusion costs something: a partition child of a CALENDAR-named parent still counts as this schema's time dimension (read from db.Partitioning.Of through the same name vocabulary the rule already applies), because a warehouse that partitions its calendar and references a specific partition — which every PostgreSQL before 12 required — loses the parent's dimension role to ADR 0033's fan-in gate, and DW-005 would otherwise claim it has no calendar at all. A genuinely PK-less table with a PROVEN-complete structure still affirms DB-050 at confidence 1.0 — honesty costs nothing here. Per-scan, sensors/db.Result.Note (reaching the agent through scan-all's DBSection.Note) carries a bounded INVENTORY of what could not be measured, aggregated by REASON never by table (a 200-table systematic gap is one line, not 200) — this is an inventory of WHAT was measured, never a DIAGNOSIS of WHY the parser failed (no parser-internal identifiers reach scan output; the closed Reason* vocabulary in core/db is the type-level control). BOUNDARY, stated so this contract does not over-promise: Complete covers DROPS, not FABRICATIONS — a reducer that believes it succeeded while inventing data (Pagila's film.fulltext phantom index, the closed \"ADD  CONSTRAINT\" double-space fabrication, and the missing-comma-before-PRIMARY-KEY wrong composite key, all documented in the SQL-DDL known limits below) reports Complete=true regardless; that class needs its own, separate control.",
		"Index access method (db.Index.Method, index-method-capture) — every index the schema-parsing providers read now carries its DECLARED access method/kind when the source states one, lowercased at every capture site for one convention across dialects: PostgreSQL's USING <method> on a standalone CREATE INDEX, before the column list (CREATE INDEX ix ON t USING gin (col)); MySQL's USING BTREE|HASH on a standalone CREATE INDEX, the DIFFERENT post-column-list position (CREATE INDEX ix ON t (col) USING BTREE|HASH); the SAME MySQL USING clause on INLINE and ALTER TABLE ADD table-constraint forms too — UNIQUE/KEY/INDEX/FULLTEXT/SPATIAL (...) USING BTREE|HASH, in EITHER grammar position (before or after the column list), at a DIFFERENT call site (applyTableConstraint) than the standalone statement, via the shared indexMethodOutsideParens helper; T-SQL's CLUSTERED/NONCLUSTERED ordinary-index kind; and T-SQL's CREATE [CLUSTERED] COLUMNSTORE INDEX kind, parsed by its OWN dedicated regex (reCreateColumnstoreIndex — a genuinely DIFFERENT statement shape, not reCreateIndex widened, since it carries no column list at all) and captured unconditionally as \"columnstore\" (the CLUSTERED qualifier is that statement's own default when omitted, so it carries no extra information beyond \"this is a columnstore index\") — its Columns is left EMPTY, never synthesized, because that statement names no column in its own grammar (it implicitly covers every column) and inventing one would misrepresent what the source actually said. Prisma's @@index(..., type: X) argument is captured the same way — DELIBERATELY not validated against any codefit-maintained vocabulary, since Prisma's own accepted set of index types has not been verified against every provider this project supports; whatever the schema text declares is captured verbatim, only lowercased. EMPTY means \"no access method declared in source\" — the same empty-means-none convention as Table.DBName — and is NEVER defaulted to a guessed value like \"btree\". This closes THREE structurally different classes of parser blindness (architecture/two-classes-of-parser-blindness, obs #1307), stated separately rather than collapsed into one: (1) DISCARD — PostgreSQL's anonymous index and T-SQL's ordinary CLUSTERED/NONCLUSTERED index were previously DROPPED entirely by reCreateIndex's grammar (T-SQL's CLUSTERED COLUMNSTORE INDEX was ALSO dropped, by a DIFFERENT statement shape reCreateIndex never covered at all) — the completeness contract (ADR 0034) already covered this honestly by marking the table unproven; this slice teaches the parser to actually READ these forms, so the abstention DISAPPEARS rather than staying reported. (2) OMISSION at the standalone CREATE INDEX call site — MySQL's post-column-list USING and Prisma's type: argument: the statement parsed successfully, only the value was silently discarded; closed by actually extracting it. (3) OMISSION at the INLINE/ALTER TABLE constraint call site (applyTableConstraint) — the SAME MySQL USING defect one level down, in a DIFFERENT function than (2), independently closed by the same extraction logic applied at that call site too. DECLARED BOUNDARY, honest abstention rather than a new capability: a PostgreSQL EXPRESSION index (CREATE INDEX ON t (lower(email))) is explicitly OUT OF SCOPE — parsing SQL expressions is not attempted. Making the anonymous-index name optional widened reCreateIndex's reach enough to also MATCH an expression index's outer parens; without a guard, its naive \\(([^)]*)\\) column-list grammar would truncate at the FIRST nested ')' and FABRICATE a phantom column literally named the truncated expression text, a regression from honest abstention to silent fabrication caught before release. The fix verifies the column-list span against balancedParen (the SAME helper applyCreateTable already uses); a mismatch routes the statement to the SAME floor a genuinely unrecognized CREATE INDEX-shaped statement uses (markUnrecognizedIndexShape), never silently proven complete. See the SQL-DDL known limits below (item 7) for the narrower set of CREATE INDEX-family shapes still genuinely unread after this slice.",
		"Paradigm and table-role detection, plus 3NF-suppression on OLAP-classified tables (S1 of the RF-03 OLAP closure; INVERTED to top-down by the SCHEMA GATE, ADR 0037) — codefit computes a schema's paradigm (oltp | olap | mixed) and each table's warehouse role (fact | dimension | staging | mart | unclassified) as a pure function of the schema, and it asks the SCHEMA first. THE SCHEMA GATE, and why it exists: 3NF-suppression (below) reads the PER-TABLE role, so before ADR 0037 a single table named dim_status with one inbound foreign key, sitting in an otherwise purely transactional schema, could silence its own DB-002/DB-003 1NF findings — the schema got no vote. It votes first now. Before any role is assigned, codefit evaluates SIX schema-wide warehouse signals and reports every one that fired, but only THREE of them decide: calendar_table (the schema declares a dedicated calendar/date/time table), surrogate_key_names (the _sk convention, 3+ such columns across 2+ tables) and type_profile_split (the schema's COLUMN TYPES split into a numeric-dominated pole plus several text-dominated ones). A schema is classified as a warehouse iff ANY ONE of those three fires. The selection is MEASURED, not reasoned: over 26 public corpora (13 analytic, 13 transactional; pinned in ADR 0036) those three fired 8/0, 3/0 and 3/0 warehouse-to-transactional — zero false positives — and requiring one of them identifies 9 of 13 warehouses, where counting instead (any 3 of the 6) identifies only 5 at the same precision. WHAT THAT MEASUREMENT COULD NOT SHOW, stated rather than implied: not ONE of the 13 transactional corpora had a table promoted to a warehouse role BEFORE the gate existed, so not one of them exhibits the hazard the gate closes (a lone dim_-named table silencing its own 1NF findings inside an otherwise OLTP schema). The corpus set therefore demonstrates the gate COST and CANNOT demonstrate its BENEFIT; the benefit is shown BY CONSTRUCTION, in the positive control and in TestSchemaGate_MovesDetect over the exact dim_status shape ADR 0035 identified (ADR 0037). The other three signals are computed and REPORTED but never vote, and the reason is the same measurement: bulk_load_shape fired on NOTHING across all 26, while no_audit_timestamps and star_topology fired 6/5 and 6/5 — near coin flips that carry almost no information and are exactly what forces a counting threshold up. If the gate is CLOSED, NO table in that schema receives a warehouse role at all, the schema folds to oltp, and the roles that WOULD have been assigned are reported rather than dropped silently (see the note below). DECLARED CONSEQUENCE of the gate's no-vacuous-truths floor: a schema of FEWER THAN 3 TABLES is never judged and therefore can never qualify — an explicit database.paradigm is the escape hatch. An OPEN gate is PERMISSION to classify, never a classification: it lets roles be assigned in that schema, and each table must still earn its own from name PLUS structure, so a schema whose gate opens but whose tables carry no recognized warehouse name at all still reads oltp with zero roles, and a recognized name structure cannot corroborate still lands in Classification.Unprovable. MEASURED on the vendored AdventureWorksDW, the only vendored corpus whose gate opens: calendar_table alone decides it (no_audit_timestamps fires too and is worth no vote), and since the reducer now reads the T-SQL ALTER TABLE ... ADD CONSTRAINT shapes that DDL uses, its 3 primary keys and 8 foreign keys corroborate the recognized PascalCase names — it classifies olap, with FactInternetSales fact and DimCustomer/DimDate dimension, Classification.Unprovable EMPTY, and the DW family evaluating it for real. INSIDE a schema that qualifies, role assignment is exactly as it was and everything that follows applies unchanged: the table NAME is the PRIMARY signal, corroborated by REAL relational structure — a fact candidate needs foreign-key fan-out to 2+ distinct tables, a dimension candidate needs to be REFERENCED (fan-in) by at least one other table. A lone single-column surrogate primary key is deliberately NOT, by itself, sufficient corroboration (nearly every ordinary OLTP table has one, so it would classify almost any fact/dim-named table as warehouse-role, fixed post-review — see ADR 0033 decision 2). Staging/mart candidates still need no structural signal in S1. THE NAME VOCABULARY, stated exactly because it decides what the whole DW family can even see, and WIDENED from the original four snake_case prefixes after an empirical yield measurement over 22 real public corpora found the family measuring near-zero on vocabulary, not on rule logic. Three spellings are recognized, all CASE-INSENSITIVELY: (a) the FIRST underscore-delimited segment — fact/fct/f → fact, dim/d → dimension, stg → staging, mart → mart (fct_ is dbt's convention, f_/d_ is dw-jobtech's); (b) the LAST underscore-delimited segment — fact/facts → fact, dim/dims → dimension (TPC-DS's date_dim, dw-supermarket's *_fact); deliberately narrower than (a), since no single-letter or staging/mart SUFFIX was observed in any corpus and an unobserved suffix is a guess, not a convention; (c) a separator-free PascalCase leading token, Fact… or Dim… (Microsoft's AdventureWorksDW spelling). DECLARED LIMITS THAT REMAIN, and they are deliberate: the PascalCase form (c) requires the token to be followed by an uppercase character AND THEN a lowercase one — \"Fact\"+\"In\" of FactInternetSales — so FactoryOrder does NOT match (\"Factory\" is not the token plus a word boundary) and an ALL-CAPS name such as FACTORY_SETTINGS or DIMENSION_CONFIG stays UNCLASSIFIED by design, because \"FACT\"+\"OR\" is genuinely ambiguous and codefit declares ambiguity rather than guessing at it; forms (a) and (b) require a real underscore, so a name with NO delimiter and no PascalCase boundary is never recognized (that delimiter is the word boundary keeping factory_settings and dimension_config out). Every one of these limits is a deliberate FALSE-NEGATIVE choice, because a false promotion here does not merely add noise — it SILENCES that table's DB-002/DB-003 1NF findings through the 3NF-suppression pass below. The widening changed NAMES only: the ADR 0033 decision-2 structural corroboration gate is untouched, and a wider name never substitutes for structure. This vocabulary is the SINGLE source for every name-shaped warehouse question codefit asks: DW-005's time-dimension name test consumes it through an exported strip-the-role-token seam rather than keeping a copy, because a copy already drifted once and turned DW-005 into a confident false claim on two real corpora (see the DW entry above). Widening the vocabulary here therefore widens what DW-005 can see, in the same change. `database.paradigm` defaults to \"auto\" (detection decides); an explicit oltp/olap/mixed value in .codefit.yaml ALWAYS wins over detection — developer autonomy is innegotiable — and that now includes OUTRANKING THE SCHEMA GATE, in one direction only (ADR 0037). An explicit olap or mixed is the developer ASSERTING this is a warehouse: it RESTORES every role a closed gate withheld, because leaving them withheld would hand the whole DW-0xx family an empty role map and silently run ZERO warehouse rules over a schema the developer just declared to be one. An explicit oltp restores NOTHING: manufacturing a warehouse role there would overrule the developer in the one direction that SILENCES findings. The gate EVIDENCE survives either override unchanged, and codefit keeps \"codefit judged this a warehouse\" and \"you told codefit this is a warehouse\" apart when it reports. Detection alone never mutates a finding; it feeds ONE first consumer this slice: DB-002 (multivalued column) and DB-003 (repeating groups) — both STILL schema-only, deterministic 1NF checks in dbrules, unchanged in shape — are SUPPRESSED by a sensor-level pass for fact/dimension/mart-role tables under auto detection or an explicit mixed override (an intentionally denormalized warehouse table is not a 1NF violation, PRD line 382), or dropped SCHEMA-WIDE under an explicit olap override (the developer asserts the whole schema is a warehouse). An explicit oltp override NEVER suppresses, regardless of any detected role — the same identical repeating-group/multivalued shape still fires exactly as before this slice. Suppression is never silent: when items are withheld, the sensor's Result.Note carries a factual trace of how many and on how many tables, plus the database.paradigm: oltp escape hatch to see them (developer autonomy is always informed of the consequence). THE GATE IS NOT SILENT EITHER, in either direction: when it CLOSES over a schema that names warehouse tables, the note states how many roles were withheld and from which tables (bounded to five names plus a count), names the three deciding signals it looked for and did not find, and names database.paradigm: olap (or mixed) as the escape hatch — this is the state a developer could not otherwise discover, since the visible consequence is 1NF items that WOULD have been suppressed simply appearing; when it OPENS, the note names WHICH deciding signals opened it, or says plainly that an explicit setting did. Both traces stay empty when the gate changed nothing. This was the FIRST OLAP-aware behavior. The star-schema/slowly-changing-dimension half of the DW-0xx family landed in S2 and IS covered — DW-001/002/005/010/011, see the entry above. The columnar/analytic-index check (DW-021) landed in S3 — see the next entry, below. The partitioning census (DW-020) landed in S4 — see the entry after it. The DW family is COMPLETE: dwrules.All() is SEVEN rules and nothing in the DW-0xx scope is left unbuilt.",
		"Fact table missing a columnar/analytic index (DW-021, S3 of the RF-03 OLAP closure) — a fact-role table with no index using a RECOGNIZED columnar/analytic access method. Pure SURFACE, never an affirmation (ADR 0017): whether the absence matters depends on this table's real size and query pattern, which codefit cannot see from static DDL — a small fact table, or one queried only by primary key, has no use for a columnar index. VOCABULARY, defined in exactly ONE place (dwrules.columnarIndexMethods) so extending it is a vocabulary change that never touches control flow — including its OWN agent-facing prose, which ReasonToReview derives from this same map rather than restating by hand: PostgreSQL contributes brin; T-SQL contributes columnstore, captured verbatim by the parser as of index-method-capture (PR #79, db.Index.Method). MySQL contributes nothing — its only index methods, btree and hash, are ordinary row-store methods, not columnar ones, so there is no MySQL-specific branch anywhere in this rule. PostgreSQL's gin/gist/spgist are DELIBERATELY excluded too (4R review, coordinator round, C1 — an architect decision, not an oversight): they are specialized lookup structures over particular data shapes (gin: full-text/array/jsonb containment; gist/spgist: geometric/range/nearest-neighbor search), not column-store or analytic-scan structures, and are siblings over overlapping workloads with no coherent \"columnar/analytic\" line separating one from the other two — admitting gin while rejecting gist/spgist would have been an arbitrary cut, so the rule admits none of the three. GATING, per table, on db.Table.StructureProven() (ADR 0034/db-model-completeness-contract) — the SAME pattern DW-001 already uses, not DW-005/DW-011's whole-rule abstain, because DW-021 is a per-table question: one fact table's dropped statement has no bearing on any other table's answer. This is the entire mechanism that makes the rule dialect-agnostic: a statement that could have declared the very columnar index this rule asks about — PostgreSQL's ON ONLY clause on a partitioned parent table's own index, the standalone CREATE FULLTEXT/SPATIAL/XML/PRIMARY XML INDEX forms, T-SQL's CREATE NONCLUSTERED COLUMNSTORE INDEX (which, unlike its CLUSTERED counterpart, carries an explicit column list — real-DDL-locked, not left as prose), and MySQL's fourth USING position (between the index name and ON) — already marks its table Complete=false via the completeness contract's existing machinery (see the SQL-DDL known limits below, item 7), so this rule's single StructureProven() gate abstains automatically on every one of those forms, with NO per-dialect branch in dw021.go itself. SIGNALS: existing_index_methods and has_any_index report EVERY index-like structure on the table, not just db.Index entries — a table keyed only by a PRIMARY KEY, with no secondary index at all, still reports has_any_index=true and names the PK in the signal (the PK carries no Method of its own, so it never satisfies the vocabulary and never changes the fire decision, but it IS an index-like structure everywhere else in this project, db.IndexLike's shared convention, and this rule's own signals must not silently disagree). The rendered list is capped (the same convention dbrules.routeUnprovenTable already uses for its own bounded signal) so a pathological table cannot balloon a single item unboundedly. This closes the S3 attempt this project shipped and then FROZE at review (sdd/db-olap-rules/s3-4r-ledger-frozen): the frozen version answered from blindness on every input path because the parser could not read index access methods at all; index-method-capture fixed the parser floor first, and this rule is deliberately nothing more than reading what the parser now provides. PRISMA IS NOT ZERO-VALUE, unlike the sibling DW-001/002/005/010/011 family (whose blanket Prisma-zero-value note concerns partitioning/materialized-view/warehouse-modelling concepts schema.prisma has no syntax for at all): Prisma's own @@index([...], type: X) syntax (the extendedIndexes preview feature) DOES let a schema declare a columnar/analytic method, and the Prisma provider captures it verbatim (lowercased) exactly like every other db.Index.Method call site — a fact-role Prisma model with @@index([col], type: Brin) genuinely suppresses this rule, proven end to end through the real Prisma provider (TestSensorDB_DW021_RealPrismaProvider). What a Prisma-only project still cannot get from this rule: T-SQL's columnstore vocabulary word has no Prisma equivalent, so a Prisma project can only ever satisfy DW-021 via brin. DOGFOOD STATUS, stated plainly: the positive and negative/trap fire paths are proven THROUGH THE REAL PARSER, not a hand-built db.Index literal — real PostgreSQL DDL parsed by sqlddl.New() for the plain-index positive and the USING brin negative, real T-SQL DDL parsed under the SQLServer dialect for the CREATE CLUSTERED COLUMNSTORE INDEX negative, and the real Prisma provider for both the no-type: positive and the type: Brin negative — plus TWO end-to-end abstention proofs against REAL genuinely-unrecognized shapes (PostgreSQL's ON ONLY, and T-SQL's CREATE NONCLUSTERED COLUMNSTORE INDEX — the same shapes internal/providers/sqlddl/testdata/pg_constructed_unrecognized_index_forms.sql and dw021_integration_test.go exercise) confirming the table lands StructureProven()=false and DW-021 emits nothing for it. DW-021's per-rule fire and trap paths are still proven by CONSTRUCTED DDL rather than by a vendored warehouse corpus, but the blindness that used to justify that is GONE: the two limits described in the S2 entry above are closed, so real vendored AdventureWorksDW now classifies olap, FactInternetSales holds a fact role, and THIS RULE FIRES ON IT — one dw-fact-no-columnar-index item, because that excerpt declares no index at all beyond its primary key (existing_index_methods reports the PK, which carries no access method and therefore never satisfies the vocabulary). That is a real end-to-end reach on third-party DDL, not a synthetic one. What the corpus still cannot carry is the NEGATIVE side — it declares no columnstore index, so the suppression path has nothing to exercise there and stays on constructed T-SQL. The statement below about vendoring is therefore narrower than it used to be, but it still holds, NOT a claim that no such corpus exists: a separate empirical yield measurement (4R review + yield measurement, coordinator round) surveyed 22 real public corpora (463 tables, 427 FKs, 771 indexes) and found DW-021 fires unmodified on 3 of them — the only DW-0xx rule that fires on unmodified real DDL, none of the five S2 rules do. That measurement's corpus is not vendored here (a future slice's job, not this one's), so DW-021's own test suite still proves its fire paths through constructed DDL, per the dogfood status above — but the coverage gap is honestly scoped to \"not vendored\", never \"does not exist\".",
		"Fact tables censused for declared table partitioning (DW-020, S4 of the RF-03 OLAP closure) — the schema's fact-role tables, counted against db.Table.Partitioning. Pure SURFACE, never an affirmation (ADR 0017): whether partitioning is worth anything depends on each table's real row count, growth rate and retention policy, which are RUNTIME facts absent from static DDL. codefit cannot see them and does not guess — it hands the agent the census and the question. SCHEMA-LEVEL, at most ONE item for the whole schema, never one per table. That is the rule's defining decision and it was MEASURED, not reasoned: across the 26-corpus survey pinned in ADR 0036, NO analytic corpus declares table partitioning at all, so a per-table rule would fire on essentially 100% of fact tables in 100% of warehouses — zero discrimination, the same defect that makes DB-052 the worst-measured rule in this project. DW-005 and DW-011 are the precedent idiom (both are schema-level census judgments); the item anchors on the FIRST unpartitioned census member in schema order and names BOTH groups in its signals the way DW-011 names both SCD groups. FIRING CONDITION: at least one census member declares no partitioning. That INCLUDES the mixed case — some fact tables partitioned, others not — deliberately, and for two reasons: an inconsistency carries MORE information than a uniform absence (the team demonstrably knows how to partition here, so the unpartitioned sibling is a decision or an oversight the agent can now judge), and suppressing on a partitioned sibling would be a counter-signal that SILENCES, the exact shape ADR 0017 forbids — one partitioned fact table would otherwise mute the question for every other fact table in the schema. The counter-signal is EXPOSED instead: partitioned_fact_tables lists them and some_fact_tables_partitioned is true, so a deliberate split is dismissible in one read. PARTITION CHILDREN ARE EXCLUDED from the census, by db.Table.Partitioning.Of. A PostgreSQL fact table partitioned into 60 monthly children yields 60 EXTRA tables in the neutral model, and a child is not a fact table; counting them would restate one partitioned fact as 61 (a CREATE TABLE c PARTITION OF p child carries its own non-empty Declaration, so it inflates the PARTITIONED side — measured by mutation) or, for a child form the parser reads without partition grammar, invert the truth by reporting a partitioned warehouse as N unpartitioned facts. This is not hypothetical: measured through the real parser and the real classifier, a child that declares its own foreign keys (which PostgreSQL 10 and earlier REQUIRED, since a partitioned parent could not carry them) comes out FACT-ROLE and would have been censused. COMPLETENESS GATING is DW-005/DW-011's whole-rule abstain (ADR 0034 §2.5), never a per-table skip: a shrunken census that still emits looks authoritative over an undercounted schema, which is a worse lie than silence. The gate reads the SAME census predicate as the census itself, and that is load-bearing rather than tidy — a PARTITION OF child is marked unproven BY CONSTRUCTION (db.ReasonPartitionChildInheritsStructure), with no parser failure involved, so gating on children would make DW-020 abstain on every declaratively partitioned PostgreSQL warehouse, i.e. be structurally incapable of ever observing its own positive case. DW-005 and DW-011 now hold to the same idiom through the shared helper in internal/core/dwrules/census.go (ADR 0039), which closes the DW-005 false negative ADR 0038 §4 measured and left open, and a DW-011 twin that ADR was wrong to rule out. THE SCHEMA GATE (ADR 0037) needs no special handling and gets none: roles are read from the classification and never re-derived, so a CLOSED gate leaves every table unclassified, the census comes out empty and the rule says nothing — asking a partitioning question of a schema codefit did not judge to be a warehouse would re-open the exact hole ADR 0037 closed. Test-locked, not assumed. DECLARED LIMITS, three of them, all inherited from the model rather than introduced here: (1) an empty Of is NOT proof a table is not a partition — a child attached by ALTER TABLE ... ATTACH PARTITION, or dumped as a standalone CREATE TABLE with no partition grammar of its own, is indistinguishable from an ordinary table, and if it earns a fact role it IS censused as unpartitioned; this is live in real DDL, since pg_dump emits exactly that form and vendored Pagila's payment_p2022_* children are all attached that way. (2) An empty Declaration is not proof a table is unpartitioned in the DATABASE — it reports the SOURCE codefit read. (3) A partitioned parent whose foreign keys live on its CHILDREN (the PostgreSQL 10 pattern) gets no fan-out of its own, so the A5 corroboration gate demotes it to unclassified and it never enters the census at all — that is role classification doing its job, and widening it here would fork the role heuristic. That third limit is no longer prose alone: it is locked through the real parser by TestDW020_RealParser_PartitionedParentWithFKsOnChildrenOnly_EmitsNothing (internal/providers/sqlddl/partition_child_census_integration_test.go), which asserts the open schema gate, the parent's proven structure, its zero foreign keys, its demotion to unclassified, the child's two foreign keys and fact role, and DW-020's resulting silence — so a change to the role heuristic cannot make this paragraph quietly false, which is exactly what ADR 0034 §2.7 requires of a declared limit. DOGFOOD STATUS, stated plainly: every fire and non-fire path is proven THROUGH THE REAL PARSER and the REAL classifier on genuine PostgreSQL DDL, never a hand-built db.Table literal — the uniform-absence positive, the mixed positive with a real fact-role PARTITION OF child (which locks both the census exclusion and the gate exemption), the every-fact-partitioned negative, the unproven-member whole-rule abstention against a real unrecognized CREATE INDEX ... ON ONLY shape, and the closed-schema-gate silence. YIELD, measured over the same 26 corpora: 8 emit exactly one DW-020 item each (dw-salesmart, dw-ssis-salesmart, dw-p4pa, dw-gamerec, dw-kantor, dw-gravity, vendored AdventureWorksDW and full AdventureWorksDW), covering 16 fact tables between them — the census halves the item count overall and collapses full AdventureWorksDW's 8 fact tables into ONE question instead of 8. The other 18 emit nothing, and for the honest reason: their schema gate is closed or they hold no fact-role table. NOT ONE analytic corpus declares table partitioning on a fact table, which is precisely the measurement that made this rule schema-level. The search behind that zero is positively controlled rather than assumed: table partitioning IS present in the corpus set, in four TRANSACTIONAL corpora (Pagila's payment, the MySQL employees_partitioned fixtures, YugabyteDB Sakila), and the parser reports it (Pagila: one declaring table) — it simply never coincides with a fact role. The window-function false positive that a naive PARTITION BY grep would have produced is real too and was excluded: dw-ngthao's only PARTITION mentions are OVER (PARTITION BY ...) query syntax, which never reaches this model. PRISMA IS ZERO-VALUE for this rule, like the S2 family and unlike DW-021: schema.prisma has no table-partitioning syntax at all, so db.Table.Partitioning is always empty on the Prisma path — a fact-role Prisma model therefore always counts as unpartitioned, which is a faithful report of what the SOURCE declares and not a claim about the database.",
	}
}

// NotCovered returns the DB dimension's explicit gaps: what codefit does not
// audit for this dimension, stated plainly rather than left silent.
func NotCovered() []string {
	return []string{
		"Index-vs-query analysis — whether the schema indexes the columns the application code actually filters on — IS now covered, by DB-010 and DB-013 (see the code×schema cross entry in Reasoning, above), for Prisma projects, inside scan-all. What remains NOT covered is the narrower set of cases the cross itself declares and abstains on: (1) RANGE vs EQUALITY — the neutral query model carries no WHERE operator at all, so a range predicate (gt/lt/contains/startsWith) is indistinguishable from an equality one; DB-013 therefore treats the filtered columns as an unordered SET and never advises on index column ORDER, which is a false-negative direction, never a wrong suggestion. (2) A column whose DECLARED type does not reveal its real cardinality — DB-010 skips only a Boolean or an enum, the two types bounded by construction, so a String used as an enum (income/expense, a role) or a nullable DateTime used as a binary flag (deletedAt: null = live) still emits and the agent refutes it by reading the schema; resolving it needs the query model to carry the WHERE's literal VALUES, a separate slice. (3) CROSS-NAMING-SPACE — the code↔schema join matches logical names EXACTLY, so a Prisma field name crossed against a physically named SQL-DDL column abstains and emits nothing (a mutilated cross is worse than an absent one). (4) CROSS-TABLE (join) filters — one query filter names ONE model, and a nested relation-filter key is dropped when the join to the schema resolves, so nothing is ever checked about a joined table's indexes. Also abstained by design: an OR/NOT clause is skipped at extraction (it does not reduce to one indexable column set), and DB-013 stays silent at four or more filtered columns (which subset to index needs selectivity codefit cannot see). SCOPE, stated plainly rather than implied: the cross needs a language provider that extracts query filters, and TypeScript/Prisma is the only one today — a Go project, or any non-Prisma data-access layer, yields no filters and therefore no cross. It runs in scan-all only; the standalone codefit-scan-db tool is schema-only and never crosses code. Whether an existing index is ACTUALLY USED at runtime is a different, telemetry-only question — see the DB-012 entry below. (N+1 query-in-loop patterns are a DIFFERENT capability again: N+1 is mapped as per-handler surface by the language provider, not by the DB dimension, so it appears in scan-all's endpoint buckets, never in this DB section.)",
		"Never-used index (DB-012) is NOT covered, and this is PERMANENT, not deferred: detecting an unused index requires runtime query telemetry (e.g. PostgreSQL's pg_stat_user_indexes) that only exists inside a live, running database with real traffic history. codefit's model is static and never connects to a database — it reads only DDL/schema text — so this rule is structurally incompatible with how codefit operates, not merely unscheduled.",
		"The routine-body rule family — DB-030 (dynamic SQL construction), DB-031 (routine without exception handling), DB-040 (trigger cross-table cascade), and DB-041 (trigger external-effecting call) — is now COMPLETE: all four are covered as surface (see Reasoning, above), none deferred. The parser prerequisite is DONE — a multi-statement T-SQL routine body is captured COMPLETE to the GO batch separator (or EOF), NOT truncated at its first internal ';' (ADR 0027; PostgreSQL dollar-quoted and MySQL DELIMITER-wrapped bodies were already complete) — which is what let these rules read whole T-SQL bodies safely; each still gates on Body.Complete so a body the parser could not prove whole is never evaluated (never a false affirmation over unread text). (Had DB-031 shipped over the OLD truncated T-SQL body, \"is exception handling present?\" would have FALSELY AFFIRMED an absence that was really just unread text past the cut — which is why the parser fix came first.) The whole family carries the same Prisma-zero-value limit as DB-020: Prisma's schema.prisma has no stored-procedure/trigger block concept, so a Prisma-only project gets no value from any of these rules.",
		"OLAP / data-warehouse RULE FAMILY (DW-0xx, CLOSED as of S4): paradigm/table-role detection, 3NF-suppression on OLAP-classified tables, the star-schema/slowly-changing-dimension checks (DW-001/002/005/010/011), the columnar/analytic-index check (DW-021) AND the partitioning census (DW-020) — ARE all covered (see Reasoning, above); dwrules.All() is SEVEN rules as of S4, and no DW-0xx rule remains unbuilt. DW-020 was the last one: its parser floor landed with partition-capture (db.Table.Partitioning) and the rule that reads it landed in S4, as a SCHEMA-LEVEL census emitting at most one item per schema. What DW-020 still cannot see is declared in its Reasoning entry rather than here, because it is a MODEL limit and not an absent rule: an ALTER TABLE ... ATTACH PARTITION child, or a child dumped as a standalone CREATE TABLE with no partition grammar of its own (what pg_dump actually emits), carries an empty Partitioning.Of and is indistinguishable from an ordinary table — so if it earns a fact role it is censused as unpartitioned. Materialized-view refresh staleness (a DW-022 candidate) was evaluated and permanently DROPPED, same DB-012 lineage as never-used-index: refresh cadence lives in external cron/scheduler state absent from static DDL, so it is structurally incompatible with codefit's static model, not merely unscheduled. Separately from the rules, the NAME-VOCABULARY limit recorded here has NARROWED: table-role detection now recognizes PascalCase Kimball naming (FactInternetSales, DimCustomer — Microsoft's own AdventureWorksDW does exactly this), underscore-delimited leading AND trailing tokens, and all of them case-insensitively (see Reasoning for the exact vocabulary). Recognizing a NAME is still not promoting a TABLE — the A5 corroboration gate is untouched and a recognized name with no supporting structure lands in Classification.Unprovable rather than in a role. AdventureWorksDW used to be the standing example of that split, and it no longer is: the T-SQL reducer now reads the ALTER TABLE ... ADD CONSTRAINT shapes that DDL uses, so its keys reach the model, structure corroborates the recognized names, and the corpus classifies olap with real fact/dimension roles that the DW rules (DW-021 included) DO evaluate. Both the vocabulary limit and the parser limit that used to block it are closed. What the vocabulary still does NOT recognize, by deliberate design rather than oversight: an ALL-CAPS name (FACTORY_SETTINGS, DIMENSION_CONFIG) — \"FACT\"+\"OR\" is genuinely ambiguous, so such a table stays unclassified and gets no DW value; and a name carrying neither an underscore nor a PascalCase Fact/Dim boundary. Both are FALSE NEGATIVES accepted on purpose, since a wrong promotion silences that table's 1NF findings. A SECOND, LARGER false-negative class was accepted with the schema gate (ADR 0037), and it is measured rather than estimated: because no table gets a warehouse role inside a schema that shows none of the three deciding warehouse signals, 4 of the 13 analytic corpora measured (dw-kenap, dw-ngthao, tpch and dw-barousse) get NO DW-0xx evaluation at all under auto detection. Only dw-barousse actually lost output to it — 10 roles and 2 DW-021 items — and it lost them to a limit already declared above rather than to a new one: its calendar is spelled dim_date_month, and the calendar signal recognizes only a role token plus exactly date/time/calendar. One line of database.paradigm: olap restores every one of these schemas in full. Every spelling the vocabulary refuses is locked as a test in internal/core/paradigm/vocabulary_test.go and internal/core/paradigm/role_token_test.go rather than left silent.",
		"SQL-DDL dialect known limits (declared, not silent): (1) a T-SQL routine body is captured to the GO batch separator (or EOF), so a CREATE TABLE-shaped fragment inside a GO-batched procedure/trigger body is absorbed into the body, NOT surfaced as a spurious top-level table (ADR 0027, closing a limit ADR 0022 had declared); the trade is that a T-SQL routine with NO trailing GO immediately followed by another statement absorbs that statement into the body — invalid T-SQL batching, the intentional boundary of ADR 0027. MySQL routine bodies wrapped in DELIMITER //...// are unaffected. (2) A MySQL client DELIMITER directive is recognized only when its argument is punctuation (//, $$); a word-based delimiter such as DELIMITER GO is NOT recognized. (3) The T-SQL GO batch separator is recognized only when the WHOLE trimmed line is the word — CASE-INSENSITIVELY, so a lowercase 'go' line is accepted exactly like 'GO' (verified against the regex and against a real warehouse script that separates its batches that way); a column literally named go alone on its own line would collide (vanishingly rare, accepted). The whole-line requirement is what keeps GOTO, or an identifier named go, from cutting a statement in half, and is deliberately NOT relaxed. (4) An inline index whose NAME is itself a type keyword (e.g. KEY int (col), an index named \"int\") is read as a column — the KEY/INDEX-vs-column discriminator trusts a type-named token as a column (pathological, accepted). (5) A column named exactly key, index, fulltext, or spatial whose type is NOT in the dialect's recognized type vocabulary (e.g. PostgreSQL's tsvector, as in real Pagila's film.fulltext column) collides with the SAME inline-index-shorthand heuristic from a different direction: the column is silently dropped and a phantom zero-column index is fabricated in its place instead. Confirmed against real vendored Pagila DDL; not yet fixed. This is a FABRICATION, not a silent drop, so db.Table.Complete (ADR 0034) cannot catch it: the reducer believes it succeeded. The completeness contract's own doc comment states this boundary explicitly (\"Complete covers DROPS, not FABRICATIONS\") rather than over-promising a guarantee this mechanism does not provide. (6) CLOSED as of tsql-alter-add-constraint, 2026-07-31 — the entry is kept (not deleted) because it names what this reducer now DOES, and because the three shapes were previously declared unparsed here. The reducer READS all three T-SQL ALTER TABLE ... ADD CONSTRAINT shapes it used to drop: T-SQL's WITH CHECK / WITH NOCHECK prefix (ALTER TABLE x WITH CHECK ADD CONSTRAINT ...); ANY whitespace run between ADD and CONSTRAINT (a newline, as formatted DDL commonly writes it, plus the two-space and tab forms SSMS emits) — the ADD item is dispatched on its leading KEYWORD, so the separator no longer decides anything; and comma-chained constraint lists (ADD CONSTRAINT a ..., CONSTRAINT b ...), where the later items repeat no verb and are read as further items of the same ADD. The SSMS tails these scripts carry are read too: a WITH (PAD_INDEX = OFF, ...) index-option list and an ON [PRIMARY] filegroup clause after the column list are both ignored without derailing the constraint, and the option list's own commas are never mistaken for the constraint separator. T-SQL's standalone CHECK CONSTRAINT / NOCHECK CONSTRAINT statement (SSMS emits one after every foreign key it generates) is a DECLARED, recognized skip — it only enables or disables checking of a constraint that already exists, so it can never declare a key, an index or a column, and it no longer demotes its table to unproven. Measured against real vendored AdventureWorksDW DDL, which uses ALL THREE shapes: 3 tables, 3/3 StructureProven, its three real primary keys and all eight real foreign keys in the model, ZERO routed db-table-structure-unproven items and an EMPTY completeness note. Locked in internal/providers/sqlddl/tsql_alter_add_constraint_test.go and internal/providers/sqlddl/dw_integration_test.go. The related FABRICATION this investigation had previously converted into a recorded drop — a non-single-space ADD/CONSTRAINT hitting the generic \"ADD \" column branch and inventing a phantom column/key literally named \"CONSTRAINT\" — is now reduced correctly instead of abstained on; the phantom column's absence stays asserted in internal/providers/sqlddl/fabrication_test.go. In its place a NARROWER floor is now enforced at the constraint level: a PRIMARY KEY / UNIQUE / FOREIGN KEY / KEY / INDEX constraint whose parenthesized column list cannot be READ (absent, unbalanced, or empty) is NOT reduced to a silently empty key — it marks the table unproven (ADR 0034), because a silently empty PrimaryKey is exactly what DB-050 reads as \"declares no primary key\" and would affirm an absence the reducer merely failed to read. (7) NARROWED as of index-method-capture, 2026-07-31: reCreateIndex now DOES parse an anonymous PostgreSQL index (CREATE INDEX ON t (...), no index name — PostgreSQL generates the name) and T-SQL's ordinary CREATE [UNIQUE] [CLUSTERED|NONCLUSTERED] INDEX form (T-SQL's everyday standalone index syntax, not an exotic shape). SEPARATELY — a DIFFERENT symbol, not reCreateIndex widened — reCreateColumnstoreIndex now parses T-SQL's CREATE [CLUSTERED] COLUMNSTORE INDEX, on its own dedicated branch, since that specific shape carries no column list at all. All three now populate db.Index.Method (see Reasoning, above) instead of dropping the statement. What STILL is NOT parsed, and still marks its table unproven exactly as described below: PostgreSQL's ON ONLY clause on a partitioned parent table's own index; the standalone CREATE FULLTEXT/SPATIAL/XML/PRIMARY XML INDEX statement forms (distinct from the inline/ADD shorthand this package already reads elsewhere); CREATE NONCLUSTERED COLUMNSTORE INDEX — unlike its CLUSTERED counterpart, a nonclustered columnstore index DOES carry an explicit column list, a materially different shape from the one this slice closed, and is deliberately left for a future slice rather than folded in here; and a FOURTH MySQL USING position — between the index name and ON (CREATE INDEX ix USING BTREE ON t (a)) — which MySQL's grammar also allows but reCreateIndex does not match, since it requires \"on\\s+\" right after the optional name group. Behavior on all these remaining shapes is CORRECT (the drop is NOT SILENT, same as (6) above): apply()'s default: branch recognizes the CREATE INDEX-shaped head and marks the table's structural completeness (db.Table.Complete=false, ADR 0034), attributing the drop to its named table when the statement's ON (or ON ONLY) clause resolves one, or to Schema.Unreduced when it does not (a wrong attribution is worse than none), and amplifies schema-wide through paradigm.Classification.Unprovable exactly as before — only this enumeration was previously incomplete (missing the fourth USING position). Locked in internal/providers/sqlddl/unrecognized_index_form_test.go (ON ONLY and the standalone FULLTEXT/SPATIAL/XML/PRIMARY XML forms), internal/providers/sqlddl/index_method_capture_test.go (the fourth MySQL USING position), and internal/providers/sqlddl/tsql_ordinary_index_completeness_test.go (the now-recognized T-SQL CLUSTERED/NONCLUSTERED form, deliberately flipped to its opposite, now-correct outcome). (8) CLOSED as of tsql-bracketed-type-names, 2026-08-01. The entry is kept (not deleted) because it names what this parser now DOES, and because the gap was previously declared here as a live blind spot. It used to read: a DELIMITED type name is not mapped — the tokenizer canonicalizes [int] to \"int\" (quoting is canonicalized for identifiers, and a type name occupies an identifier position), the type vocabulary is keyed on the bare word, so the lookup missed and the column's neutral type fell back to db.TypeUnknown. That was a REAL blind spot on the exact form Microsoft's own generated scripts use for every column ([CustomerKey] [int] IDENTITY(1,1) NOT NULL), and its measured cost was DW-002 firing on BOTH DimCustomer and DimDate even though CustomerKey and DateKey ARE single-column integer surrogates. The column-type lookup now UNWRAPS the canonical delimiters before consulting the dialect's TypeMap (internal/providers/sqlddl/types.go, typeLookupKey), so [int] / `int` / \"int\" all classify exactly like the bare word. The fix is dialect-FREE and closes all three dialects at once — the tokenizer had already collapsed every dialect's quoting onto one canonical form, so there is no bracket strip and no per-dialect branch anywhere; MySQL backtick-quoted and ANSI double-quoted type names were probed on main and confirmed to carry the SAME defect, and the SAME one-line seam closes them. What did NOT change, deliberately: RawType still carries the source spelling verbatim; typeBase itself is untouched, so the KEY/INDEX-vs-column discriminator (limit (4)) still reads a delimited token as an index NAME rather than a type; and an UNRECOGNIZED keyword still falls back to db.TypeUnknown — the fix maps a delimited SPELLING onto the same lookup key, it never widens the vocabulary or guesses. A form that is not exactly ONE quoted identifier ([dbo].[MyType], canonicalized to \"dbo\".\"MyType\") is left untouched and stays TypeUnknown rather than being half-stripped. MEASURED over 26 corpora, both directions: exactly two corpora moved, and only DW-002's item count changed anywhere — 26 items to 8, all 18 removed items belonging to the two AdventureWorksDW corpora, ZERO items added by any rule. The vendored 3-table excerpt goes from 74 of 74 columns unclassified to 0 of 74; the full upstream install script (not vendored here) from 359 of 359 to 6 of 359, and those 6 are the honest fallback still working ([sysname] x5, [xml] x1 — real T-SQL types outside the declared vocabulary). The 8 surviving DW-002 items are text/varchar-keyed dimensions the rule should fire on. The schema gate's type_profile_split signal (ADR 0036) newly REACHES both corpora: it fires on the full install script, and on the 3-table excerpt it is now evaluated and returns false on the arithmetic (one text-dominated table against a floor of two) instead of failing closed. No gate VERDICT changed on any corpus. No golden file changed, because all three golden fixtures write their types undelimited — which is exactly why this was invisible until a real Microsoft script was parsed end to end. (9) CLOSED as of run-on-statement-separation, 2026-08-02 (ADR 0041) — the entry is kept rather than deleted because what it describes was the LAST silent structural loss in this parser, and because what replaced it has a boundary of its own. Two CREATE TABLE statements with NO separator between them (neither ';' nor a GO batch line) used to reduce to only the FIRST table, SILENTLY: the second table simply did not exist in the model, the first was left StructureProven, and nothing was recorded in Schema.Unreduced or the completeness note. T-SQL makes the statement terminator optional, so this is valid input, not malformed DDL. The reducer now SEPARATES the run: after a table's body is located with balanced parentheses, its TAIL is scanned for a CREATE/ALTER/DROP keyword at paren depth 0 that is outside any string literal and outside any quoted identifier, the statement is cut there, and the remainder is dispatched as a statement in its own right, recursively — so a run of N tables recovers all N, each with its own source line. Measured through the real parser on a public warehouse script: 1 table becomes 7, with 41 columns and 7 foreign keys, all structure-proven. The keyword set is exactly those three words BECAUSE none of them is legal at the top level of a CREATE TABLE tail in any of the three dialects, while WITH and SET are (WITH (autovacuum_enabled = off), WITH (DATA_COMPRESSION = PAGE)) — so a residual that begins with SELECT, INSERT or any other word is still not detected and is still lost silently, unchanged by this fix and stated here rather than implied. What the fix guarantees is the OTHER half: nothing is recovered while anything DETECTED is lost in silence — a residual the boundary rule found but no dispatch branch reduces (CREATE TYPE, CREATE SEQUENCE, ALTER SCHEMA, …) is appended VERBATIM to Schema.Unreduced and reaches the agent through the per-scan inventory, and the host table is never demoted for it (its own body was read in full, so demoting it would be a false demotion). Blast radius measured across 26 external corpora: exactly ONE corpus changes, the other 25 are identical on tables, structure-proven count, columns, foreign keys, indexes, views, procedures, triggers, paradigm and every emitted item; no golden was regenerated. (10) TWO CREATE TABLE statements whose SCHEMA-QUALIFIED names differ ONLY by schema (bronze.orders and silver.orders) COLLAPSE INTO ONE table: the reducer keys the model on the BARE table name, so the second statement targets the table the first created, and the columns unique to the second never enter the model. UNLIKE the run-on loss limit (9) describes — which was silent until ADR 0041 closed it — this loss is NOT silent, and the difference matters: the colliding statement is recorded in that table Unreduced list and the table is marked Complete=false, so DB-050 ROUTES it to db-table-structure-unproven and DB-001/DB-052 plus all seven DW-0xx rules ABSTAIN rather than reading the merged shape as fact. MEASURED through the real parser, not inferred: three CREATE TABLE statements across bronze/silver/gold reduce to TWO tables, the surviving orders carrying only the FIRST statement columns. NOT fixed: keying the model by schema-qualified name is a neutral-model change (db.Table would have to carry its schema), deliberately outside this slice. (11) NARROWED as of partition-capture, 2026-08-01: table PARTITIONING was previously ignored outright — a PostgreSQL/MySQL PARTITION BY tail and a T-SQL ON <scheme>(<column>) clause were dropped without a trace while the table stayed proven, and a PostgreSQL PARTITION OF child statement matched NO dispatch branch at all, so the entire child table vanished from the model. The reducer now READS all three into db.Table.Partitioning: the strategy word the source spells (PARTITION BY RANGE|LIST|HASH|KEY|LINEAR HASH|RANGE COLUMNS|…, lowercased, no closed vocabulary), the partition key when it is a plain column list, the T-SQL partition scheme name plus the strategy resolved through its own CREATE PARTITION FUNCTION when that statement is in the DDL read, and a partition child's parent table. A child is modelled as ITS OWN TABLE plus a back-reference (Partitioning.Of), never folded into the parent, and is marked unproven (see the completeness entry above). What is READ but NOT DECOMPOSED, reported through Partitioning.Declaration — the clause verbatim — with the structured fields left EMPTY rather than guessed: an EXPRESSION partition key (PARTITION BY RANGE (YEAR(sold_on)), extract(...)), because splitting one with the ordinary column-list splitter fabricates a column name that exists in no table, and db.Table.Complete cannot catch a fabrication; MySQL's KEY ALGORITHM=2 strategy form; MySQL SUBPARTITION BY; and the individual partition names and bounds. What is NOT READ AT ALL and routes to the pre-existing honest-abstention floor exactly as before: PostgreSQL ALTER TABLE ... ATTACH/DETACH PARTITION and MySQL ALTER TABLE ... PARTITION BY, which mark their table Complete=false — so an attached child is never LINKED to its parent, and Partitioning.Of being empty is not proof a table is not a partition. Neither is Declaration being empty proof a table is unpartitioned: pg_dump emits most partition children as ordinary standalone CREATE TABLEs with no partition grammar of their own (the vendored payment_p2022_01 is exactly that), and codefit reports the SOURCE, not the database. NO vendored corpus declares table partitioning at all — every PARTITION occurrence under testdata/ is inside a COMMENT — so this read is proven by constructed DDL plus two real-corpus NEGATIVE controls: AdventureWorksDW's ') ON [PRIMARY];' filegroup clause on all three tables, which must not read as a partition scheme, and a corpus-wide sweep asserting no table in any of the 17 corpora reports partitioning. Locked in internal/providers/sqlddl/partition_capture_test.go. (12) CLOSED as of missing-comma-constraint-separation, 2026-08-02 (ADR 0042) — the entry is kept rather than deleted because what it described was a FABRICATION rather than a drop, and because the rule that closed it has a declared boundary of its own. A CREATE TABLE body item MISSING its separating comma before a table-level key constraint used to read that constraint as an INLINE one on the preceding column: 'Profit INT<newline>PRIMARY KEY(Car_sid, Date_from)' yielded PrimaryKey=[Profit] instead of the declared composite, while the table still reported Complete=true. Like limit (5) that is a FABRICATION, not a drop, so the completeness contract could not catch it — the reducer believed it succeeded. It was delimiter-independent (reproduced on an ordinary ';'-terminated statement, so it had nothing to do with run-on separation) and was found while measuring ADR 0041, which made it REACHABLE on real DDL for the first time: a public warehouse script writes its fact table exactly this way. The reducer now CUTS the body item at the constraint head and reduces both halves, because the grammar DECIDES the reading rather than leaving it to a guess: in PostgreSQL, MySQL and T-SQL alike an INLINE PRIMARY KEY/UNIQUE takes no bare parenthesized column list (WITH (...), INCLUDE (...), USING INDEX TABLESPACE, T-SQL's ON scheme (...) always intervene) and FOREIGN KEY (...) is not a column-constraint form at all (T-SQL's inline FOREIGN KEY REFERENCES t (c) always puts a table name before the paren), so '<column definition> <head> (' has exactly ONE legal reading. Heads recovered, each also with a CONSTRAINT <name> preamble: PRIMARY KEY [CLUSTERED|NONCLUSTERED] (...), UNIQUE [CLUSTERED|NONCLUSTERED] (...), UNIQUE KEY|INDEX [name] (...), FOREIGN KEY [name] (...). The host column is NOT demoted — nothing was dropped — and a residual whose column list cannot actually be read still falls to the pre-existing MarkUnproven floor. MEASURED over 29 corpora (the 26-corpus external survey ADR 0041 used, which already includes verbatim copies of the vendored fixtures, plus three jobs covering every .sql corpus under this repo testdata): exactly three changed, all in the SAME direction, a fabricated single-column key replaced by the declared one — dw-kenap's Fact_Reservation from [Profit] to its six declared columns, and dw-salesmart / dw-ssis-salesmart's dim_date from [calendar_month_name] to [date_key] / [date_sk] — which removed one false DB-001 and two false DW-002 items. Table counts, structure-proven counts, columns, indexes, foreign keys, views, procedures, triggers, column RawTypes and paradigm were byte-identical on all 29. WHAT REMAINS NOT COVERED, each a decision rather than an oversight, and each locked as a characterization test in internal/providers/sqlddl/missing_comma_test.go: (a) MySQL's bare KEY/INDEX/FULLTEXT/SPATIAL secondary-index shorthand — a bare KEY is ALSO a legal inline column modifier (it means PRIMARY KEY), and cutting on it would fabricate on a column whose user-defined TYPE is named key ('b key(10)'), so that index is still lost and the preceding column's RawType still absorbs the clause; (b) CHECK and EXCLUDE, which have a legal inline reading and declare no key, index or column either way, so both readings reduce to the same model; (c) PRIMARY KEY USING BTREE (...) and UNIQUE NULLS NOT DISTINCT (...), whose extra tokens the closed optional set deliberately does not admit; (d) a missing comma between two PLAIN column definitions, which no keyword marks at all — the two still read as one column. A SEPARATE fabrication of the same class was closed in the same change and is not a parser limit but a scan discipline: applyColumn's inline-constraint scans now read the column's modifier tail MASKED to top level, so a keyword inside a string literal, a quoted identifier or a parenthesized expression is data rather than syntax (a COMMENT string reading PRIMARY KEY no longer declares a key, and one reading NOT NULL no longer reports the column non-nullable). It changed nothing on any of the 29 corpora. (13) CLOSED as of table-shaped-head-floor, 2026-08-02 (ADR 0043), and it closes a CLASS rather than a list of forms. Before it, a CREATE <anything> TABLE head that no dispatch branch reduced fell through apply()'s default: and left NO trace of any kind — no table in the model, no Schema.Unreduced entry, no note. MEASURED through the real Sensor.Audit, not inferred: a schema whose only statement is a CREATE UNLOGGED TABLE audited as Measured=true, Note empty, tables=0, findings=0, surface=0 — the false 'audited, 0 findings' state over DDL codefit never read, which is the worst state an auditor can be in because it is indistinguishable from a clean bill of health. TWELVE forms were confirmed silent this way, each under the dialect it belongs to: PostgreSQL's UNLOGGED and UNLOGGED ... IF NOT EXISTS, TEMP, TEMPORARY, GLOBAL TEMPORARY and LOCAL TEMPORARY; MySQL's TEMPORARY; T-SQL's #Local and ##Global name prefixes; plus CREATE FOREIGN TABLE, CREATE TABLE ... AS SELECT, and a table whose quoted name falls outside the reducer's identifier class. CREATE TABLE IF NOT EXISTS was never affected — it is an explicit group in the regex and has always worked. The fix is the structural analogue of what reIndexShapedHead has done for the CREATE INDEX family since ADR 0034: a LAST-RESORT net after every real table branch, so a form nobody has taught this parser yet is DECLARED instead of evaporating. THREE dispositions, deliberately different because they are different facts about the schema. (a) MODELED — CREATE UNLOGGED TABLE, admitted to the model like any other table: an unlogged table only skips the write-ahead log, it is ordinary persistent storage and its keys are as real as any other's. (b) WITHHELD — the TEMP/TEMPORARY family and T-SQL's '#'/'##' name prefix. These are READ correctly and deliberately left OUT of the model: a temporary table is dropped with the session that created it, so it is not part of the persistent schema, and admitting it would have DB-050 affirm 'table without a primary key' over session scratch space at confidence 1.0. Withholding is never silent — it is recorded on Schema.Withheld with the core's closed WithheldReason vocabulary, the declared name and the verbatim statement, and reported in the per-scan note as its OWN trace, aggregated by reason and capped at five names, never one line per table. It is deliberately NOT reported through Schema.Unreduced: that channel means 'codefit could not read this', and using it for a scoping decision would tell an agent the parser failed where it in fact succeeded. (c) DECLARED — every other table-shaped head, recorded verbatim on Schema.Unreduced and reaching the agent through the per-scan inventory, exactly as ADR 0041's found residual does. THE CATCHER NEVER GUESSES A NAME, and that is a decision rather than an omission: unlike a CREATE INDEX form, which always carries an ON clause a drop can be attributed to, the forms landing here are by definition ones whose grammar this reducer does not know, so it does not know where their name sits either (a CREATE TABLE ... AS SELECT puts it in one position, a CREATE FOREIGN TABLE ... SERVER in another). Registering a table from a guessed span would be the FABRICATION class db.Table.Complete structurally cannot catch — strictly worse than the silence it replaces — so the verbatim statement plus its file:line is what carries the name to the agent. The catcher's modifier window is at most TWO words between CREATE and TABLE, and that bound is load-bearing: it admits every real one- and two-word form (UNLOGGED, FOREIGN, EXTERNAL, TRANSIENT, GLOBAL TEMPORARY, LOCAL TEMP, OR REPLACE, SET/MULTISET) while excluding three-word shapes that are NOT table declarations — T-SQL's CREATE TYPE x AS TABLE (a user-defined table TYPE), CREATE STATISTICS s ON t, and CREATE SCHEMA s CREATE TABLE ... (the SQL-standard element list). MEASURED over the same 29 corpora ADR 0042 used: ZERO delta on tables, structure-proven counts, columns, foreign keys, indexes, views, procedures, triggers, paradigm, every emitted item and the scan note; the three schema goldens gained one additive key (Withheld) and nothing else. A zero delta is exactly what a broken harness also produces, so the measurement was proven SENSITIVE with positive controls: widening the catcher's window to THREE words moves adventureworks-oltp-pg (5 new Schema.Unreduced entries from its CREATE SCHEMA element lists), making the UNLOGGED prefix MANDATORY moves 22 of 29 corpora with table counts collapsing, and admitting TEMP/TEMPORARY into reCreateTable puts session scratch into the model on the authored fixture. WHAT REMAINS NOT COVERED, each a decision rather than an oversight: (i) T-SQL's '#' recognition is proven by CONSTRUCTED DDL only. The form does occur in the external survey — dw-gravity's 'DWH Scripts/1.1_CreateDimDate.sql' declares CREATE TABLE #tmpHoliday(...) at the top level of a batch script — but that occurrence is unreachable for an unrelated, PRE-EXISTING reason, confirmed through the real parser rather than assumed: the preceding UPDATE is unterminated, so the whole run is one statement whose head is UPDATE and no head regex ever sees the CREATE. That is the run-on class, not this one. (ii) A THREE-word modifier form (MariaDB's CREATE OR REPLACE TEMPORARY TABLE) falls outside the catcher's window and stays silent. (iii) CREATE SCHEMA s CREATE TABLE ... remains the open gap ADR 0041 recorded: it is deliberately outside the window, because admitting it would start declaring statements on a corpus that is read correctly today. (iv) Withholding SHRINKS the modeled table count, which can move a small schema below the schema gate's minJudgeableTables floor of 3 and silence every warehouse signal. That is the correct reading — session scratch space is not evidence about whether a schema is a warehouse — but it is a real consequence, measured on the authored T-SQL fixture and recorded in its schema-gate corpus row. (v) A later ALTER TABLE or CREATE INDEX naming a withheld temporary table still materializes a phantom table carrying ReasonTableNeverDeclared, exactly as before: withholding removes the CREATE from the model, it does not teach the other branches that the name is temporary.",
		"SQL-DDL dialect assumptions: MySQL parsing assumes ANSI_QUOTES is OFF (a bare \" is read as a string literal, not an identifier quote); the parser binds a single dialect per project at construction (a project mixing dialects is not supported). A project mixing .prisma and .sql schema inputs is likewise out of scope.",
	}
}
